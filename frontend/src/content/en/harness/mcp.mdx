---
title: MCP Integration
description: Versioned system and project MCP assets with project-owned secrets.
---

# MCP Integration

MCP definitions are immutable database asset versions. System admins publish system versions; project members create project versions or bind approved system versions.

Each Project stores encrypted values by exact MCP Version and slot. A definition never contains a secret. Gateway admission freezes the exact MCP Version and Secret Generation for a Run; Worker materializes it only at the call boundary and records quota/audit there.

Manage system MCP assets in the admin asset console and project MCP assets in project settings. Viewer access remains read-only when permitted by capability.
