[project]
name = "deer-flow"
version = "2.1.0"
description = "ActWeave — Weave intelligence into action. A LangGraph-based AI agent system with sandbox execution."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deerflow-harness",
    "alembic>=1.16.0,<2",
    "fastapi>=0.115.0",
    "httpx>=0.28.0",
    "python-multipart>=0.0.31",
    "sse-starlette>=2.1.0",
    "uvicorn[standard]>=0.34.0",
    "lark-oapi>=1.4.0",
    "slack-sdk>=3.33.0",
    "python-telegram-bot>=21.0",
    "langgraph-sdk>=0.1.51",
    "markdown-to-mrkdwn>=0.3.1",
    "wecom-aibot-python-sdk>=0.1.6",
    "dingtalk-stream>=0.24.3",
    "bcrypt>=4.0.0",
    "pyjwt>=2.13.0",
    "email-validator>=2.0.0",
    "e2b-code-interpreter>=2.8.1",
    "python-socks[asyncio]>=2.8.2,<3",
]

[project.optional-dependencies]
discord = ["discord.py>=2.7.0"]

[dependency-groups]
dev = [
    "prompt-toolkit>=3.0.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "ruff>=0.14.11",
    # TUI runtime dep (also declared as the deerflow-harness[tui] extra); kept in
    # the dev group so the terminal workbench can be run and tested locally / in CI.
    "textual>=0.80",
]

[tool.pytest.ini_options]
markers = [
    "postgres: tests that require an isolated PostgreSQL test database",
]

[tool.uv]
index-url = "https://pypi.org/simple"

[tool.uv.workspace]
members = ["packages/harness"]

[tool.uv.sources]
deerflow-harness = { workspace = true }
